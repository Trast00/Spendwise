<header>
  <nav class="flex-center navbar">
    <%= link_to image_tag("icons8-gauche-50 (1).png", width: 32), category_payments_path(@category), class: 'no-styling flex-center' %>
    <a href=<%= category_payments_path(@category) %> class="flex-center max-content-flex"><h1>New payment</h1></a>
  </nav>
</header>

<%= render "form", payment: @payment %>