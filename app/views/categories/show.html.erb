<p style="color: green"><%= notice %></p>

<%= render @category %>

<div>
  <%= link_to "Edit this categorie", edit_category_path(@category) %> |
  <%= link_to "Back to categories", categories_path %>

  <%= button_to "Destroy this categorie", @category, method: :delete %>
</div>
